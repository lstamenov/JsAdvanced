let sumInverse = (first,  second, third) => {
    let sum = 0;
    sum += a + b + c;
    console.log(sum);
    sum = 0;
    sum += (1/a) + (1/b) + (1/c);
    console.log(sum);
    let stringSum = '';
    stringSum.concat(a );
    console.log(stringSum);
}

console.log(sumInverse(1, 2 ,3));